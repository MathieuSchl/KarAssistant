const gameWidth = 200.0;
const gameHeight = 200.0;
const fingerRadius = gameWidth * 0.04;
const batWidth = gameWidth * 0.5; // Add from here...
const batHeight = fingerRadius * 0.4;
const batStep = gameWidth * 0.05;
